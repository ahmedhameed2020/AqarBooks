// Re-exported from lib/units/unit-type-labels.ts, which is the canonical
// home for this shared label map (portal code under app/[locale]/portal/**
// depends on lib/, never on this (app) tree -- see that file for why).
export { UNIT_TYPES, OCCUPANCY_STATUSES, UNIT_TYPE_LABELS, unitTypeLabel, type UnitType } from "@/lib/units/unit-type-labels";
